# RESUME HERE — 2026-08-27 (supersedes RESUME_2026-08-26.md)

Working tree CLEAN. Everything below is committed and pushed to
`claude/session-recap-j7evnx`.

## THE ONE THING WAITING ON THE USER
A clean (no-profiler) three-lever build is flashed to the user's board. The
number to read is **`B4dur quiet`** — does it fit under 5804 µs? Predicted
~5883 µs (still ~79 µs over). That decides how big the subset VCA move must be.

## WHAT LANDED THIS SESSION (all proven, all pushed)

### JUNO port correctness (every source file now audited)
- 4 real `juno_ramp.c` defects fixed; `make verify` GREEN.
- Control layer + all remaining files audited (2 workflow passes): no live
  defect. One LATENT `carp.c` finding recorded (`docs/CARP_LATENT_FINDING.md`).
- Two audit false-claims of mine withdrawn under adversarial refutation.

### Headroom (O4 steady-state) — three bit-exact levers
| lever | file | flag | proof |
|---|---|---|---|
| A/R/Rk dead math | `eb_vcf_ladder.c` | `#if !EB_HALF_OS_VCF` | compile-dead + make engineb |
| dco_live dead fields | `eb_render.c` | `#if !EB_DCO_WT ...` | forkbit 0/4.3M |
| L-B LFO tail CR | `eb_lfo.*` | `EB_LFO_TAIL_CR` | forkbit 0; tooth RED 2.9M |
| L-A set_pitch CR | `eb_render.c`,`eb_dco_wt.h` | `EB_DCO_WT_LIVE_CR` | forkbit 0; never-call tooth RED 3.3M |

Silicon: **~118 µs saved at idle** (b30/b31). Narrows the +197 µs deficit to
~79 µs. Does NOT close it — the **subset VCA move is still required.**

### New tooling (reusable)
- `tools/engineb/forkbit.py` — the fork-vs-fork BIT runner b24 said was
  missing. Proves a fork lever changes 0 output bits. Seen to fail first.
  **RULE: a prologue-side lever must be gated with `EB_SPLIT_TEST` set, or the
  gate reaches nothing (found the hard way — playbook 80).**
- `tools/engineb/shim_drift_tooth.py` — `make engineb` step 1b; catches a fork
  drifting from `src/` (the expf class), seen to fail.
- `tools/verify/gate_parity.py` + `docs/GATE_PARITY.tsv` — JUNO/JX gate parity.
- `tools/run_job.sh` heartbeat; playbooks 82/82b/83.

## THE NEXT STEP, precisely (board-free, then ONE flash)
1. Read the clean-build `B4dur quiet` (user flashing now).
2. **Build the subset VCA move** — b26 plan, sized to the measured 0.542 and to
   the remaining deficit. Flag `S3L_VCA_PIPE`. Cut set carries `glide[v].s560`
   (7 floats); `eb_modcv_latch` STAYS on core 0 (b24 §6).
3. **Prove it bit-exact on HOST first.** The chunk-late move is "same samples,
   one chunk later", so a byte-for-byte compare needs a ONE-CHUNK OFFSET —
   forkbit compares aligned, so either extend it with an offset or add an
   `EB_SPLIT_TEST`-style host driver that runs the deferred VCA in order. This
   is the one real design task remaining for the move.
4. ONE flash: confirm `quiet` fits and `underruns=0`.

## OWED (smaller, do when convenient)
- A glide/portamento scenario in `null_b` — the battery has NO fast pitch
  motion, so a wrong-period tooth passed vacuously (b30). Real hole for future
  pitch-path levers. Adding one needs care: verify it ACTUALLY glides
  (portamento on + legato) or it gives false confidence.
- L-A part: move the 9-field recall copy into the seed block (`latecopy`
  counter). Small extra saving.
- Micro-levers (wrap-swap 25, nsvf/vcf_res dead stores) — diminishing returns,
  each fiddly/conditional. Low priority.

## THE MAP TO THE END GOAL (FINAL_GUIDE tracks)
O4 (headroom) — IN PROGRESS, subset VCA move is the remaining piece.
O5 controls/LCD/preset · O6 finish the six-voice link (mix=OPEN already proven)
· F1 stress green · F2 the user listens and accepts. ~4 more flashes, each a
milestone, because every feature is host-proven before it is flashed.
