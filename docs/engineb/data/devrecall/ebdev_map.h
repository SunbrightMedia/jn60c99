/* GENERATED: literal-constant binary-search map. Folds to one load at any
 * call site whose offset is a compile-time constant. */
#define EBDEV_MAP_BODY \
      if (off < 2418432u) { \
          if (off < 524288u) { \
              if (off < 131072u) { \
                  if (off < 95824u) { \
                      if (off >= 84272u && off < 91732u) return EBS.sg + 0u + (off - 84272u); \
                      goto miss; \
                  } else { \
                      if (off < 101024u) { \
                          if (off >= 95824u && off < 96932u) return EBS.sg + 7460u + (off - 95824u); \
                          goto miss; \
                      } else { \
                          if (off >= 101024u && off < 102804u) return EBS.sg + 8568u + (off - 101024u); \
                          goto miss; \
                      } \
                  } \
              } else { \
                  if (off < 192000u) { \
                      if (off < 154360u) { \
                          if (off >= 131072u && off < 131076u) return EBS.sg + 10348u + (off - 131072u); \
                          goto miss; \
                      } else { \
                          if (off >= 154360u && off < 154364u) return EBS.sg + 10352u + (off - 154360u); \
                          goto miss; \
                      } \
                  } else { \
                      if (off < 262144u) { \
                          if (off >= 192000u && off < 192004u) return EBS.sg + 10356u + (off - 192000u); \
                          goto miss; \
                      } else { \
                          if (off >= 262144u && off < 262148u) return EBS.sg + 10360u + (off - 262144u); \
                          goto miss; \
                      } \
                  } \
              } \
          } else { \
              if (off < 1048576u) { \
                  if (off < 600000u) { \
                      if (off < 588912u) { \
                          if (off >= 524288u && off < 524292u) return EBS.sg + 10364u + (off - 524288u); \
                          goto miss; \
                      } else { \
                          if (off >= 588912u && off < 588916u) return EBS.sg + 10368u + (off - 588912u); \
                          goto miss; \
                      } \
                  } else { \
                      if (off < 842520u) { \
                          if (off >= 600000u && off < 600004u) return EBS.sg + 10372u + (off - 600000u); \
                          goto miss; \
                      } else { \
                          if (off >= 842520u && off < 842524u) return EBS.sg + 10376u + (off - 842520u); \
                          goto miss; \
                      } \
                  } \
              } else { \
                  if (off < 2097152u) { \
                      if (off < 1499996u) { \
                          if (off >= 1048576u && off < 1048580u) return EBS.sg + 10380u + (off - 1048576u); \
                          goto miss; \
                      } else { \
                          if (off >= 1499996u && off < 1500000u) return EBS.sg + 10384u + (off - 1499996u); \
                          goto miss; \
                      } \
                  } else { \
                      if (off < 2199952u) { \
                          if (off >= 2097152u && off < 2097156u) return EBS.sg + 10388u + (off - 2097152u); \
                          goto miss; \
                      } else { \
                          if (off >= 2199952u && off < 2199972u) return EBS.sg + 10392u + (off - 2199952u); \
                          goto miss; \
                      } \
                  } \
              } \
          } \
      } else { \
          if (off < 6496496u) { \
              if (off < 4297120u) { \
                  if (off < 4194304u) { \
                      if (off < 2509804u) { \
                          if (off >= 2418432u && off < 2418436u) return EBS.sg + 10412u + (off - 2418432u); \
                          goto miss; \
                      } else { \
                          if (off >= 2509804u && off < 2509808u) return EBS.sg + 10416u + (off - 2509804u); \
                          goto miss; \
                      } \
                  } else { \
                      if (off < 4195232u) { \
                          if (off >= 4194304u && off < 4194308u) return EBS.sg + 10420u + (off - 4194304u); \
                          goto miss; \
                      } else { \
                          if (off >= 4195232u && off < 4195300u) return EBS.sg + 10424u + (off - 4195232u); \
                          goto miss; \
                      } \
                  } \
              } else { \
                  if (off < 6429408u) { \
                      if (off < 6395248u) { \
                          if (off >= 4297120u && off < 4298100u) return EBS.sg + 10492u + (off - 4297120u); \
                          goto miss; \
                      } else { \
                          if (off >= 6395248u && off < 6396644u) return EBS.sg + 11472u + (off - 6395248u); \
                          goto miss; \
                      } \
                  } else { \
                      if (off < 6463712u) { \
                          if (off >= 6429408u && off < 6430948u) return EBS.sg + 12868u + (off - 6429408u); \
                          goto miss; \
                      } else { \
                          if (off >= 6463712u && off < 6463732u) return EBS.sg + 14408u + (off - 6463712u); \
                          goto miss; \
                      } \
                  } \
              } \
          } else { \
              if (off < 10691936u) { \
                  if (off < 8594768u) { \
                      if (off < 8388608u) { \
                          if (off >= 6496496u && off < 6497620u) return EBS.sg + 14428u + (off - 6496496u); \
                          goto miss; \
                      } else { \
                          if (off >= 8388608u && off < 8388612u) return EBS.sg + 15552u + (off - 8388608u); \
                          goto miss; \
                      } \
                  } else { \
                      if (off < 8999996u) { \
                          if (off >= 8594768u && off < 8594788u) return EBS.sg + 15556u + (off - 8594768u); \
                          goto miss; \
                      } else { \
                          if (off >= 8999996u && off < 9000000u) return EBS.sg + 15576u + (off - 8999996u); \
                          goto miss; \
                      } \
                  } \
              } else { \
                  if (off < 10758864u) { \
                      if (off < 10726256u) { \
                          if (off >= 10691936u && off < 10693492u) return EBS.sg + 15580u + (off - 10691936u); \
                          goto miss; \
                      } else { \
                          if (off >= 10726256u && off < 10726276u) return EBS.sg + 17136u + (off - 10726256u); \
                          goto miss; \
                      } \
                  } else { \
                      if (off < 11022032u) { \
                          if (off >= 10758864u && off < 10759876u) return EBS.sg + 17156u + (off - 10758864u); \
                          goto miss; \
                      } else { \
                          if (off >= 11022032u && off < 11022352u) return EBS.sg + 18168u + (off - 11022032u); \
                          goto miss; \
                      } \
                  } \
              } \
          } \
      } \
