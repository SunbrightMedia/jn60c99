# RESUME HERE — session ended 2026-08-26 (context limit)

Working tree was CLEAN at handoff. Everything below is committed and pushed to
`claude/session-recap-j7evnx`.

## 1. RUNNING JOBS — check these FIRST
    sh tools/run_job.sh --list
Report ONLY from `bench/jobs/<name>/EXIT`. A job with no EXIT whose pid is gone
DIED — the row says so, with the heartbeat age.

| job | state at handoff | what it proves |
|---|---|---|
| `jx_full_ftz` | **FINISHED ok (EXIT=0)** | recall 64/64 + render 64/64 x 3 rates, all EXACTLY 0, WITH the FTZ fix |
| `juno_verify` | RUNNING 30m | **whether today's `src/juno_ramp.c` fixes regress shipped JUNO code. THE gating question.** |
| `juno_survivors` | RUNNING 38m | confirms 5 mutation survivors under full `make verify` |

Also running: a 12-agent Workflow auditing the JUNO control layer for the same
defect class (run id `wf_0376accd-905`). If its result was lost, re-run it —
the script is saved and re-invocable by `scriptPath`.

## 2. WHAT WAS FIXED TODAY (all committed)

### JX-3P — floating-point MODE defect
The port computed WITHOUT SSE FTZ/DAZ while the plugin (and oracle) compute
WITH it. `jx3p/src/jx_ftz.c` added. Worse, `ab_render_c.py` already had
`if hasattr(lib,"jx_set_ftz")` — a symbol the library never exported, so the
branch was ALWAYS false. It now REFUSES to run without it.
Found by seeds, not by the bank: 90/200 random cases diverged.

### JUNO — four defects in `src/juno_ramp.c` (see docs/RAMP_AB_FINDINGS.md)
1. **`juno_ramp_reset` never cleared `step_cnt`** — no NaN involved. A
   re-armed ramp fired its first increment up to `subdiv-1` ticks EARLY.
   Seen to fail: removing this alone drops the gate 300/300 -> 215/300.
2-4. three unordered-compare mistranscriptions (playbook 81).
My FIRST fix for those was ALSO WRONG and the gate caught it: `ucomiss` sets
ZF=1 on unordered, so `jne` is not taken for NaN just as for equal. Neither
`a != b` nor `!(a != b)` works — only ordered-and-unequal (`JR_NE` macro).

## 3. NEW MACHINERY (all seen to fail before being believed)
| tool | purpose |
|---|---|
| `tools/verify/ramp_ab_gate.sh` | JUNO ramp A/B. 300/300 over 3 seeds x 24 steps |
| `jx3p/tools/ramp_walk_gate.sh` | JX ramp walker. 2000 cases, ~25k ramps |
| `jx3p/tools/fuzz_gate.sh` | JX seeded fuzz — the gate the JX never had |
| `tools/verify/gate_parity.py` | RED when one port has a gate class the other lacks |
| `tools/verify/disasm.py --cmp` | every compare + the unordered behaviour of its jump |
| `tools/run_job.sh` heartbeat | a killed job now reports HOW LONG it ran |

## 4. OPEN, NAMED, NOT GUESSED
- **`jx3p/docs/FUZZ_SEED2_OPEN.md`** — JX fuzz seed 2 diverges (96 kHz,
  patch 7, warm=2, n=75). UNATTRIBUTED. Replay: `sh jx3p/tools/fuzz_gate.sh 2 3`.
  Run the CONTROL first (charter rule 7) before calling it a port defect.
- **`docs/GATE_PARITY.tsv` is RED: 10 owed gates**, all JX-side.
- **JUNO control layer beyond the ramp** — `juno_note.c`, `juno_apply.c` are
  the same layer and were never audited (playbook 83). The workflow above
  targets exactly this.
- JUNO idx 878 / 1029 ("float twins") are claimed proven in prose with NO
  gate. A claim without a tooth is what failed us before.

## 5. THE USER'S STANDING DIRECTION (2026-08-26, binding)
**Finish the JUNO — port, trunk, AND fork — before any more JX work.**
No JX work depends on the JUNO. The dependency runs the other way: JX gates
exposed the JUNO's missing equivalents, which is how the ramp defects surfaced.
The 5-step plan is `docs/PLAN_5_STEPS.md`; the user's priority reorders it so
JUNO Step 4 (the S3 fork, Tracks A-E) comes before JX Steps 2-3.

## 6. FIRST THREE ACTIONS NEXT SESSION
1. `sh tools/run_job.sh --list` — read `juno_verify`'s EXIT. If non-zero, the
   ramp fixes regressed something and that is the whole job.
2. Collect the control-layer workflow result; fix only CONFIRMED findings.
3. Resume the JUNO fork (FINAL_GUIDE.md Tracks A-E): O3 is BUILT, GATED and
   COMPILED but UNFLASHED; O4's lever is the t5 arithmetic vs the +197 us.
