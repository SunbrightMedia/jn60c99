# esp32s3/ -- the S3 FORK firmware (F4 preparation; see main/juno_s3_main.c
# for what it runs and what it measures).
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(juno_s3)
