What: proved the KEY ASSIGN blind spot — recall never called the assigner
notify, so port AND oracle played every patch in POLY (16/64 factory patches
wrong); also mapped Boost/Output-Gain as engine no-ops.
Cited by: docs/ASSIGNER_MODE_FINDING.md (primary), docs/VOICE_MODES.md,
docs/HOSTPATH_PARITY_SCOPE.md. Gate born from it: tools/verify/assigner_ab.py.
